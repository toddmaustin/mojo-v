Various dev notes:

Usage Notes:
 - Bringup-Bench now has the top-level "make mojov-clean" and "make mojov-tests" to clean all Mojo-V directories and run tests
 - IMPORTANT: secret registers can never breach x15 or lower or f15 or lower, without making the short/implied-reg format compressed instructions safe!


FIXMEs:
 - currently the Spike implementation is not compatible with EXT_ZFINX (single-precision floating-point instructions using the integer register file), enforce this!
 - currently Vector instructions are not supported and will leak secret register values, fix this!

UPSTREAMS:
 - test out the non-long long version of dopr() on the base bringup-bench
 - add the exception dump code for all RISC-V targets
 - to see all changes since start of project:
   git log d4251067d663e33df422b965e2137f67a7048baf..HEAD -p -- riscv-isa-sim/

Conifgure commands:

Spike: 
  ../configure --prefix=$RISCV

Safe disclosures:

  Datagrant:

// datagrant memory format
union mojov_mem_datagrant_t {
  struct {               // ciphertext
    uint128_t ct_lo;       // ciphertext low 128-bits
    uint128_t ct_hi;       // ciphertext high 128-bits
  } ct;
  struct { // 256-bits in size, 128-bit alignment
    uint64_t dfhash; // datagrant dataflow hash value
    uint64_t salt; // random salt
    uint64_t auth_sig; // authentication signature (from contract) PLUS ONE
    uint64_t metadata; // target-specific metadata (e.g., device hash, overflow flag)
  } pt;
};

Disclosure Instruction:

disc rd,rs,rt
  rd - x1-s23  - disclosed 64-bit integer value
  rs - x24-x31 - secret value to (attempt to) disclose
  rt - ?? 
